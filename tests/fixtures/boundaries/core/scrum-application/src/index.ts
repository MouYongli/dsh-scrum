// `application-defines-ports-only`: the application reaches a Harness plugin.
// `no-cross-package-file-import`: it does so by a relative path into that
// package's files rather than by its package name.
export { host } from '../../../harness/scrum-harness-host/src/index.js'
