/**
 * Browser half of the bundle. The module loader serves the built form of this
 * entry under `/plugins`, so it must be reachable from the same package the
 * loader row names.
 *
 * @module @dsh-scrum/scrum-harness-bundle/client
 */
export { apply, inject, name } from '@dsh-scrum/scrum-harness-client/client'
